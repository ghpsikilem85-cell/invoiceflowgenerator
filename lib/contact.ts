/**
 * The public contact address. Namecheap includes free email forwarding with
 * the domain, so this can point at a personal inbox without publishing it.
 */
export const CONTACT_EMAIL =
  process.env.NEXT_PUBLIC_CONTACT_EMAIL ?? "support@invoiceflowgenarator.com";
